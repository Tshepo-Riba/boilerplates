[defaults]
inventory = ./hosts
roles_path = roles

[ssh_connection]
ssh_args = -o ControlMaster=auto -o ControlPersist=60s

[privilege_escalation]
become = true
become_method = sudo
become_user = root
